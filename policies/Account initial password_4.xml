<?xml version="1.0" encoding="UTF-8"?>
<rule>
									<description>Account initial password</description>
									<comment xml:space="preserve">GroupWise has two passwords, initial password and regular password.  
In GroupWise the initial password is stored in clear text and can be seen by an admin.
The regular password is encrypted and can not be viewed.
When set, the regular password is used by GroupWise instead of the initial password. 
When a GroupWise user changes his password, it is stored as the regular password.
In the interest of security, the initial password is never set to a password sent from eDirectory (nspmDistributionPassword attribute).
The "Synchronize the eDirectory password to the GroupWise regular password" Global Config Value controls setting the regular password from a password in eDirectory.
The initial password is set by the shim when it sees a &lt;password&gt; element as part of an &lt;add&gt;.  
The regular password is set by the shim when it sees a &lt;modify-password&gt; command.</comment>
									<conditions>
										<and>
											<if-class-name op="equal">User</if-class-name>
											<if-global-variable name="pwdsync.set-initial-password" op="equal">true</if-global-variable>
										</and>
									</conditions>
									<actions>
										
										<do-set-dest-password>
											<arg-string>
												<token-attr name="Surname" />
												<token-text>-</token-text>
												<token-attr name="CN" />
											</arg-string>
										</do-set-dest-password>
									</actions>
								</rule>
							