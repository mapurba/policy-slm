<?xml version="1.0" encoding="UTF-8"?>
<rule>
									<description>Strip association-ref attribute from User object 'memberOf' values</description>
									<comment xml:space="preserve">For Jade implementation, Group objects will not be imported into the IDV so the association-ref of the group which comes from the driver shim must be stripped off.</comment>
									<conditions>
										<and>
											<if-class-name mode="nocase" op="not-equal">group</if-class-name>
										</and>
									</conditions>
									<actions>
										<do-strip-xpath expression="*/value/@association-ref | */*/value/@association-ref" />
										<do-strip-xpath expression="*/value[@type='dn']/@type | */*/value[@type='dn']/@type" />
									</actions>
								</rule>
							