<?xml version="1.0" encoding="UTF-8"?>
<rule>
									<description>Validate required match attributes</description>
									<comment xml:space="preserve">Validate that a User has the requisite attributes needed for synchonizing to Sentinal as a Sentinel Identity.

Note that this policy checks for attributes needed in the User match policy. All attributes required by Sentinel are either synthesized or required on the eDir User (e.g., GUID is required in eDir, Distinguished Name is synthesized).</comment>
									<conditions>
										<or>
											<if-xpath op="not-true">count($drv.matching_attrs[.=$current-op/add-attr/@attr-name])=count($drv.matching_attrs)</if-xpath>
										</or>
									</conditions>
									<actions>
										<do-veto />
									</actions>
								</rule>
							