<?xml version="1.0" encoding="UTF-8"?>
<rule>
									<description>Street Address: Convert LF to CR-LF</description>
									<comment xml:space="preserve">The ILM Vault Street Address typically uses the Unix end-of-line convention (a single new line character) while Active Directory uses the Windows convention (carriage return plus new line characters). This rule converts values to the Windows format so that the synchronized values display properly in both systems.</comment>
									<conditions>
										<and />
									</conditions>
									<actions>
										<do-reformat-op-attr name="streetAddress">
											<arg-value>
												<token-replace-all regex="[^\r]\n" replace-with="\r\n">
													<token-local-variable name="current-value" />
												</token-replace-all>
											</arg-value>
										</do-reformat-op-attr>
									</actions>
								</rule>
								