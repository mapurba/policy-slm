<?xml version="1.0" encoding="UTF-8"?>
<rule>
									<description>Prepare to check role entitlements after add</description>
									<comment xml:space="preserve">Role entitlements are processed after the add completes because the added object needs to get roles. The add is flagged with an operational property that is checked in the input transform when add processing is complete.</comment>
									<conditions>
										<and>
											<if-global-variable mode="nocase" name="drv.entitlement.Role" op="equal">true</if-global-variable>
											<if-class-name op="equal">User</if-class-name>
											<if-entitlement name="Role" op="available" />
										</and>
									</conditions>
									<actions>
										<do-set-op-property name="check-role-entitlements">
											<arg-string>
												<token-text>true</token-text>
											</arg-string>
										</do-set-op-property>
									</actions>
								</rule>
								