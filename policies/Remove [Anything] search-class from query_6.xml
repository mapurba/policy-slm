<?xml version="1.0" encoding="UTF-8"?>
<rule>
									<description>Remove [Anything] search-class from query</description>
									<comment xml:space="preserve">Sometimes a query will get sent to the shim that contains [Anything] as a search class. This class doesn't exist in SharePoint and the intent of using that search-class is to NOT limit the query, so this search-class should be stripped.</comment>
									<conditions>
										<or>
											<if-operation mode="case" op="equal">query</if-operation>
										</or>
									</conditions>
									<actions>
										<do-strip-xpath expression="search-class[@class-name='[Anything]']" />
									</actions>
								</rule>
							