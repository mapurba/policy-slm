# 10.1 Platform Configuration File Location

The location of the platform configuration file depends on which operating system the platform is using.

For Linux and UNIX, the default configuration file is /usr/local/ASAM/data/asamplat.conf. You can use a command line parameter to specify a different platform configuration file. More information can be found in [Section IV, Platform Services Administration](bexh69n.html).
