# 2.5 Starting ActiveMQ

To start an ActiveMQ instance, navigate to <activemq unzipped path>/bin and perform one of the following actions:

* *Linux:*
  Run the./activemq start command.
* *Windows:*
  Run the.activemq.bat start command.
