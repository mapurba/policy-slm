# 7.13 Fanout Agent Stop Processing Events

When a JDBC driver instance is not responding because of an irresponsive database, the events for the database are not processed until the database returns to a normal state.
