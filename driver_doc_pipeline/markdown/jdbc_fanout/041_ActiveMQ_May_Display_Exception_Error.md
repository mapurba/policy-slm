# 7.4 ActiveMQ May Display Exception Error

ActiveMQ may display an exception error while it is viewing a queue with an unprocessed event. This issue is randomly observed.

To workaround this issue, install the latest patch from the [ActiveMQ web site](https://issues.apache.org/jira/browse/AMQ-5356).
