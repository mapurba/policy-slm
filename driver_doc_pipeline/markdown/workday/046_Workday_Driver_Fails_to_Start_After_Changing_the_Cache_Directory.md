# 9.3 Workday Driver Fails to Start After Changing the Cache Directory

Workday driver fails to start after changing the cache directory in the driver configuration parameters or cache files are removed from the existing location.

You can start the driver after some time of changing the cache directory.
